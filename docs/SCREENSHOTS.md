# Screenshots

Drop demo screenshots into this folder and reference them from the README.

Recommended captures:

- `dashboard.png` — Main dashboard with live webcam + detected sign
- `sentence.png`  — Generated sentence with speech controls
- `history.png`   — History page with search and delete
- `training.png`  — (Optional) Terminal running `ml/train.py` with the
  classification report

PNG/JPG, ≤ 1 MB each.
